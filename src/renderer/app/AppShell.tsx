import React from 'react';
import { EmpaquetarShell } from './EmpaquetarShell';

/** Producto EMPAQUETAR standalone: sin HUD de AI Studio. */
export const AppShell: React.FC = () => {
  return <EmpaquetarShell />;
};
